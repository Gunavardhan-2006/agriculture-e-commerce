import { notFound } from "next/navigation"; import { Navbar } from "@/components/shared/Navbar"; import { listings } from "@/lib/demo-data"; import { ListingDetail } from "@/components/buyer/ListingDetail"; import { SuggestedProducts } from "@/components/buyer/SuggestedProducts"; export default async function ListingPage({params}:{params:Promise<{listingId:string}>}){const {listingId}=await params;const listing=listings.find(x=>x.id===listingId);if(!listing)return notFound();return <><Navbar/><ListingDetail listing={listing}/><SuggestedProducts listing={listing}/></>}
