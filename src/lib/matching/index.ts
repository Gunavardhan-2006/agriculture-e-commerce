import type { Listing, MatchBreakdown } from "@/types";
export type Requirement = { category?: string; qty: number; grade?: string; budget?: number; deadline?: string; maxDistance?: number };
export const matchListings = (source: Listing[], r: Requirement) => source.filter(x=>x.status==="active" && (!r.category || x.category===r.category)).map(listing=>{ const price=r.budget ? Math.max(0,Math.min(25,25-(Math.max(0,listing.price-r.budget)/r.budget)*50)) : 20; const quantity=Math.min(25,(listing.quantity/r.qty)*25); const quality=!r.grade||listing.grade===r.grade?20:10; const distance=r.maxDistance?Math.max(0,15-(listing.distance/r.maxDistance)*15):Math.max(0,15-listing.distance/20); const timeline=!r.deadline||listing.availableUntil>=r.deadline?15:0; const breakdown:MatchBreakdown={price:Math.round(price),quantity:Math.round(quantity),quality,distance:Math.round(distance),timeline,total:Math.round(price+quantity+quality+distance+timeline)}; return {listing,breakdown}; }).sort((a,b)=>b.breakdown.total-a.breakdown.total);
