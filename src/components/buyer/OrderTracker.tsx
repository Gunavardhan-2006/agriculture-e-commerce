import { Check, Circle } from "lucide-react";
const stages=["Placed","Confirmed by farmer","Harvest & pack","Handed to transporter","In transit","Delivered"];
export function OrderTracker({current=2}:{current?:number}){return <div className="space-y-0">{stages.map((stage,index)=><div key={stage} className="flex gap-3"><div className="flex flex-col items-center"><span className={`grid h-7 w-7 place-items-center rounded-full ${index<=current?"bg-emerald-700 text-white":"border border-stone-300 bg-white text-stone-400"}`}>{index<=current?<Check size={15}/>:<Circle size={12}/>}</span>{index<stages.length-1&&<span className={`h-8 w-px ${index<current?"bg-emerald-700":"bg-stone-200"}`}/>}</div><div className="pb-4"><p className={`text-sm font-bold ${index<=current?"text-slate-900":"text-slate-400"}`}>{stage}</p><p className="mt-0.5 font-mono text-[11px] text-slate-500">{index===current?"Current update · 11:42":index<current?"Completed": "Awaiting update"}</p></div></div>)}</div>}
