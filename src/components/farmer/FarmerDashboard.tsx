import Link from "next/link"; import { AlertTriangle, ArrowUpRight, Package, Plus, TrendingUp } from "lucide-react"; import { Card } from "@/components/ui/Card"; import { Badge } from "@/components/ui/Badge"; import { listings } from "@/lib/demo-data"; import { formatCurrency } from "@/lib/utils"; import { priceAdvisory } from "@/lib/pricing";
export function FarmerDashboard(){const mine=listings.slice(0,3);const advice=priceAdvisory(28,30);return <main className="mx-auto max-w-7xl px-4 py-7 sm:px-6"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="font-mono text-xs uppercase tracking-wider text-emerald-700">Farmer desk · Ramesh</p><h1 className="mt-2 font-display text-3xl font-bold">Good morning. Here&apos;s your farm board.</h1></div><Link href="/dashboard/listings/new" className="inline-flex items-center gap-2 rounded-lg bg-emerald-700 px-4 py-3 text-sm font-bold text-white"><Plus size={17}/> New listing</Link></div><div className="mt-7 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{[[Package,"3","Active listings","emerald"],[TrendingUp,"₹18,430","Est. earnings","amber"],[ArrowUpRight,"2","Orders in progress","emerald"],[AlertTriangle,"1","Stock running low","orange"]].map(([Icon,value,label,tone])=>{const I=Icon as typeof Package;return <Card key={String(label)} className="p-4"><I className={tone==="orange"?"text-orange-600":"text-emerald-700"} size={19}/><p className="mt-5 font-mono text-2xl font-bold">{String(value)}</p><p className="mt-1 text-sm text-slate-600">{String(label)}</p></Card>})}</div><div className="mt-7 grid gap-5 lg:grid-cols-[1.3fr_.7fr]"><Card className="overflow-hidden"><div className="flex items-center justify-between border-b border-stone-200 p-5"><h2 className="font-display text-xl font-bold">Your active stock</h2><Link href="/dashboard/listings" className="text-sm font-bold text-emerald-800">Manage stock</Link></div><div className="divide-y divide-stone-100">{mine.map(x=><div key={x.id} className="flex items-center justify-between gap-3 p-4"><div><p className="font-bold">{x.variety}</p><p className="mt-1 font-mono text-xs text-slate-500">{x.quantity} kg left · {x.harvest}</p></div><div className="text-right"><p className="font-mono text-sm font-bold text-emerald-800">{formatCurrency(x.price)}/kg</p><Badge tone="emerald">LIVE</Badge></div></div>)}</div></Card><Card className="p-5"><p className="font-mono text-xs uppercase tracking-wider text-emerald-700">Price advisory</p><h2 className="mt-2 font-display text-xl font-bold">Tomato market position</h2><div className="mt-5 flex items-end gap-3"><span className="font-mono text-3xl font-bold text-emerald-800">₹28</span><span className="pb-1 text-sm text-slate-500">your listing / kg</span></div><p className="mt-2 text-sm text-slate-600">Regional average is <span className="font-mono">₹30/kg</span>. Your price is {Math.abs(advice.delta)}% below market.</p><div className="mt-4 rounded-lg bg-amber-50 p-3 text-sm leading-5 text-amber-950">Demand is trending up and your produce has 4 days of shelf life left — consider listing now.</div></Card></div></main>}
