"use client"; import { useState } from "react"; import { Button } from "@/components/ui/Button";
export function ListingForm(){const [error,setError]=useState("");const submit=(form:FormData)=>{const quantity=Number(form.get("quantity"));const start=String(form.get("from")),until=String(form.get("until"));if(quantity<=0)return setError("Quantity must be greater than zero.");if(!until||new Date(until)<=new Date(start)||new Date(until)<new Date())return setError("Available-until must be after available-from and cannot be in the past.");setError("");alert("Listing validated and saved in demo mode.")};return <form action={submit} className="grid gap-5"><label className="text-sm font-semibold">Produce variety<input name="variety" defaultValue="Hybrid Tomato" className="mt-2 w-full rounded-lg border border-slate-300 p-3 font-normal"/></label><div className="grid gap-4 sm:grid-cols-3"><label className="text-sm font-semibold">Quantity<input name="quantity" type="number" defaultValue="100" className="mt-2 w-full rounded-lg border border-slate-300 p-3 font-normal"/></label><label className="text-sm font-semibold">Unit<select className="mt-2 w-full rounded-lg border border-slate-300 p-3 font-normal"><option>kg</option><option>quintal</option><option>tonne</option></select></label><label className="text-sm font-semibold">Grade<select className="mt-2 w-full rounded-lg border border-slate-300 p-3 font-normal"><option>Grade A</option><option>Grade B</option><option>Grade C</option></select></label></div><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold">Available from<input name="from" type="date" defaultValue="2026-09-14" className="mt-2 w-full rounded-lg border border-slate-300 p-3 font-normal"/></label><label className="text-sm font-semibold">Available until<input name="until" type="date" defaultValue="2026-09-18" className="mt-2 w-full rounded-lg border border-slate-300 p-3 font-normal"/></label></div><label className="text-sm font-semibold">Expected price / kg<input type="number" defaultValue="28" className="mt-2 w-full rounded-lg border border-slate-300 p-3 font-normal"/><span className="mt-2 block font-mono text-xs text-emerald-800">Regional suggested price: ₹30/kg</span></label><div className="rounded-lg border border-amber-200 bg-amber-50 p-3 text-sm text-amber-950">Farm location: Chevella, Telangana. Complete your profile before publishing if this is not correct.</div>{error&&<p className="rounded bg-red-50 p-3 text-sm text-red-700">{error}</p>}<Button type="submit">Publish listing</Button></form>}
